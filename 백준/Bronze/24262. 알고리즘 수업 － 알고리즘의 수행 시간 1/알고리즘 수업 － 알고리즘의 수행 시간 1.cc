#include <bits/stdc++.h>
using namespace std;

int MenOfPassion(int A[], int n) {
    int i = n / 2;  // floor(n / 2)
    return A[i];
}

int main() {
    int n;
    cin >> n;

    cout << 1 << '\n'; // 코드1의 수행 횟수
    cout << 0 << '\n'; // 다항식의 최고차항 차수

    return 0;
}